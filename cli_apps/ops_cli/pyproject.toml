[project]
name = "ops-cli"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
  "typer",
  "common",
]

[tool.uv.sources]
common = { workspace = true }

[build-system]
requires = ["uv-build"]
build-backend = "uv_build"

[project.scripts]
ops-cli = "ops_cli.cli:app"
